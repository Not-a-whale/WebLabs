import Meals from "./Meals";
import {Table} from "../Layout/Table";

export const MealsContainer = (props) => {

    return (
        <main>
            <Meals />
            <Table />
        </main>
    )
}